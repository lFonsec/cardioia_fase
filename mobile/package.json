{
  "name": "cardioia-mobile",
  "version": "1.0.0",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "scripts": { "start": "expo start", "android": "expo start --android", "build:apk": "eas build -p android --profile preview" },
  "dependencies": { "expo": "latest", "react": "latest", "react-native": "latest", "@expo/vector-icons": "latest" },
  "devDependencies": { "eas-cli": "latest" }
}
